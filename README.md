# Sinus graph

1. Sinus function is generated on the server and sent using the ws package.
2. Client on React Vite receives sinus points from the server via websocket and displays them on a graph using the recharts library.
